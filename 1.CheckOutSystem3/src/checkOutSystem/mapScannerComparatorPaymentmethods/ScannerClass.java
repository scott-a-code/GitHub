package checkOutSystem.mapScannerComparatorPaymentmethods;

import static java.lang.System.in;

import java.util.Scanner;

public class ScannerClass {

	public static final Scanner scanner = new Scanner(in);
}
	/*
	 * public static final void getScanner() { try (Scanner scanner = new
	 * Scanner(in)) { } catch (InputMismatchException ex) { } return; } }
	 * 
	 * /*return scanner;
	 * 
	 * public static final Scanner scanner = new Scanner(in);
	 * 
	 * try { scanner = new Scanner(in); } catch (Exception ex) {
	 * ex.printStackTrace(); } finally { scanner.close(); } }
	 */

